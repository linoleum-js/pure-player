
/// <reference path="../../typings/jasmine.d.ts" />

import Track from '../../src/Track';

export default function runTrackTest () {
  describe('player', () => {
    it('should be ok', () => {
      expect(true).toBe(true);
    });

    it('should fail', () => {
      expect(true).toBe(true);
    });
  });
}
